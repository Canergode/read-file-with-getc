# Read File With getc

This project reads and prints all characters from a text file using getc inside a do-while loop, stopping when the end of file is reached.

## What I Learned
- How to open a file in read mode
- How to read characters one by one using getc
- How to use EOF to detect the end of a file
- How to print characters to screen using putchar
